{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window capability — app-specific permissions are in meta-plot.json",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
